<UserControl x:Class="BEditor.Views.SettingsControl.Plugins.InstalledPlugins"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BEditor.Views.SettingsControl.Plugins"
             xmlns:properties="clr-namespace:BEditor.Core.Properties;assembly=BEditor.Core"
             xmlns:app="clr-namespace:BEditor"
             xmlns:component="clr-namespace:BEditor.Models"
             xmlns:vm="clr-namespace:BEditor.ViewModels.SettingsControl.Plugins"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="{DynamicResource MaterialDesignPaper}">
    <UserControl.DataContext>
        <vm:InstalledPluginsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox ItemsSource="{Binding LoadedPlugins, Source={x:Static component:AppData.Current}}"  HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" SelectedItem="{Binding SelectPlugin, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Height="60">
                        <VirtualizingStackPanel>
                            <Label Content="{Binding PluginName}" FontSize="14"/>
                            <Label Content="{Binding Infomation}"/>
                        </VirtualizingStackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <VirtualizingStackPanel Grid.Row="1" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button Margin="10" Content="{x:Static properties:Resources.Settings}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource MaterialDesignBody}" Command="{Binding SettingClick, Mode=OneTime}"/>
        </VirtualizingStackPanel>
    </Grid>
</UserControl>
