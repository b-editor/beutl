<UserControl x:Class="BEditor.Views.PropertyControls.CheckBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:convert="clr-namespace:BEditor.ViewModels.Converters"
             mc:Ignorable="d" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignCardBackground}"
             d:DesignWidth="583"
             Height="32.5">
    <UserControl.Resources>
        <convert:PropertyHeaderTextConverter x:Key="PropertyHeaderTextConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120*" MaxWidth="100" MinWidth="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*" MinWidth="100"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <TextBlock Text="{Binding Property.PropertyMetadata.Name, Mode=OneTime}" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="13" Margin="10,0,0,0" TextTrimming="CharacterEllipsis">
                <TextBlock.ToolTip>
                    <MultiBinding Converter="{StaticResource PropertyHeaderTextConverter}">
                        <Binding Path="Property.PropertyMetadata.Name" Mode="OneTime"/>
                        <Binding Path="Property.Parent.Name" Mode="OneTime"/>
                        <Binding Path="Property.ClipData.LabelText" Mode="OneWay"/>
                    </MultiBinding>
                </TextBlock.ToolTip>
            </TextBlock>
        </Grid>

        <CheckBox Grid.Column="2" IsChecked="{Binding Property.IsChecked, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:InvokeCommandAction Command="{Binding Command, Mode=OneTime}" CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=CheckBox}}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </CheckBox>


        <md:PopupBox Width="20" Grid.Column="4" StaysOpen="False" Height="32.5" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Stretch">
            <md:PopupBox.ToggleContent>
                <md:PackIcon Kind="DotsVertical" Width="16" Height="16" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            </md:PopupBox.ToggleContent>
            <VirtualizingStackPanel>
                <Button Content="Reset"/>
            </VirtualizingStackPanel>
        </md:PopupBox>
    </Grid>
</UserControl>
