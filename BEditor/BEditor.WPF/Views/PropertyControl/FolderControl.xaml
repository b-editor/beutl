<UserControl x:Class="BEditor.Views.PropertyControl.FolderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BEditor.Views.PropertyControls"
             xmlns:convert="clr-namespace:BEditor.ViewModels.Converters"
             mc:Ignorable="d" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignCardBackground}"
             d:DesignWidth="583"
             x:Name="root" Height="42.5">
    <UserControl.Resources>
        <convert:PropertyHeaderTextConverter x:Key="PropertyHeaderTextConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120*" MaxWidth="100" MinWidth="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*" MinWidth="100"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <TextBlock Text="{Binding Metadata.Value.Name, Mode=OneTime}" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="13" Margin="10,0,0,0" TextTrimming="CharacterEllipsis">
                <TextBlock.ToolTip>
                    <MultiBinding Converter="{StaticResource PropertyHeaderTextConverter}">
                        <Binding Path="Metadata.Value.Name" Mode="OneTime"/>
                        <Binding Path="Property.Parent.Name" Mode="OneTime"/>
                        <Binding Path="Property.Parent.Parent.LabelText" Mode="OneWay"/>
                    </MultiBinding>
                </TextBlock.ToolTip>
            </TextBlock>
        </Grid>

        <!--text box-->
        <TextBox Grid.Column="3" VerticalAlignment="Center" Margin="5,0,5,0" Text="{Binding Property.Folder, Mode=OneWay}" VerticalContentAlignment="Center" IsReadOnly="True"/>

        <!--button-->
        <Button Margin="10,0,0,0" Grid.Column="2" Padding="0,0,0,0" Background="{x:Null}" BorderBrush="{x:Null}" Command="{Binding Command, Mode=OneTime}" CommandParameter="{Binding ElementName=root, Path=FolderChangeCommand}" HorizontalAlignment="Left" Width="32" Cursor="Hand">
            <md:PackIcon Kind="Folder" Foreground="{DynamicResource MaterialDesignBody}"/>
        </Button>

        <md:PopupBox Width="20" Grid.Column="4" StaysOpen="False" Height="32.5" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" VerticalAlignment="Stretch">
            <md:PopupBox.ToggleContent>
                <md:PackIcon Kind="DotsVertical" Width="16" Height="16" Foreground="{DynamicResource MaterialDesignBody}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
            </md:PopupBox.ToggleContent>
            <VirtualizingStackPanel>
                <Button Content="Reset" Command="{Binding Reset, Mode=OneTime}"/>
                <Button Content="Bind" Click="BindClick"/>
            </VirtualizingStackPanel>
        </md:PopupBox>
    </Grid>
</UserControl>
