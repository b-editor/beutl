``` diff
BEditor.Audio
-public abstract class AudioLibraryObject : IDisposable
-{
-    ~AudioLibraryObject();

-    public abstract int Handle { get; }
-    public bool IsDisposed { get; }

-    public void Dispose();
-    protected void ThrowIfDisposed();
-    protected abstract void OnDispose();
-}

-public enum AudioSourceState
-{
-    Initial,
-    Playing,
-    Paused,
-    Stopped,
-}

-public enum AudioSourceType
-{
-    Static,
-    Streaming,
-    Undetermined,
-}

-public sealed class AudioSource : AudioLibraryObject
-{
-    public AudioSource();

-    public override int Handle { get; }
-    public AudioSourceState State { get; }
-    public AudioSourceType SourceType { get; set; }
-    public float Pitch { get; set; }
-    public float Gain { get; set; }
-    public int SampleOffset { get; set; }
-    public int BuffersQueued { get; }
-    public int BuffersProcessed { get; }
-    public int ByteOffset { get; set; }

-    public void Play();
-    public void Stop();
-    public void Pause();
-    public void QueueBuffer(AudioBuffer buffer);
-    public void QueueBuffer(params AudioBuffer[] buffer);
-    public int UnqueueBuffer();
-    public void UnqueueBuffer(int count);
-}

-public sealed class AudioBuffer : AudioLibraryObject
-{
-    public AudioBuffer(Sound<PCM16> sound);
-    public AudioBuffer(Sound<StereoPCM16> sound);
-    public AudioBuffer(Sound<PCMFloat> sound);
-    public AudioBuffer(Sound<StereoPCMFloat> sound);
-    public AudioBuffer();
-    public AudioBuffer(int handle);

-    public override int Handle { get; }
-    public int Frequency { get; }
-    public int Bits { get; }
-    public int Channels { get; }
-    public int Size { get; }

-    public void BufferData(Sound<PCM16> sound);
-    public void BufferData(Sound<StereoPCM16> sound);
-    public void BufferData(Sound<PCMFloat> sound);
-    public void BufferData(Sound<StereoPCMFloat> sound);
-    protected override void OnDispose();
-}

BEditor.Base
public struct EditingPropertyOptions<TValue>
{
-   public EditingPropertyOptions<TValue> Serialize(Action<Utf8JsonWriter, TValue> write, Func<JsonElement, TValue> read);
}

public class EditingPropertySerializer<TValue>
{
-   public EditingPropertySerializer(Action<Utf8JsonWriter, TValue> write, Func<JsonElement, TValue> read);
}

BEditor.Core
public class Blend
{
-   public void ResetOptional();
}

public class Coordinate
{
-   public void ResetOptional();
}

public class Rotate
{
-   public void ResetOptional();
}

public class Scale
{
-   public void ResetOptional();
}

public record EasePropertyMetadata
{
-   public EasePropertyMetadata(string Name, EasingMetadata DefaultEase, float DefaultValue = 0, float Max = float.NaN, float Min = float.NaN, bool UseOptional = false);
-   public EasePropertyMetadata(string name, float defaultValue = 0, float max = float.NaN, float min = float.NaN, bool useOptional = false);
+   public EasePropertyMetadata(string Name, EasingMetadata DefaultEase, float DefaultValue = 0, float Max = float.NaN, float Min = float.NaN);
+   public EasePropertyMetadata(string name, float defaultValue = 0, float max = float.NaN, float min = float.NaN);

-   public bool UseOptional { get; init; }
}

public class EaseProperty
{
-   public float Optional { get; set; }
}

public class FileProperty
{
-   public string RawValue { get; }
}

public class FolderProperty
{
-   public string RawValue { get; }
}

public class ClipElement
{
+   public event EventHandler<ClipSplittedEventArgs> Splitted;

-   public string LabelText { get; set; }

-   public IRecordCommand ChangeLength(Frame start, Frame end);
}

public class Scene
{
-   public string SceneName { get; set; }
-   public DrawingContext DrawingContext { get; }
-   public float TimeLineZoom { get; set; }
}

public class Project
{
+   public static readonly EditingProperty<DateTime> LastSavedTimeProperty;

+   public DateTime LastSavedTime { get; }
}

public sealed class ClipApplyArgs
{
-   public ClipApplyArgs(Frame frame, ApplyType type = ApplyType.Edit);
}

public class EffectApplyArgs
{
-   public EffectApplyArgs(Frame frame, ApplyType type = ApplyType.Edit);
}

public class EffectApplyArgs<T>
{
-   public EffectApplyArgs(Frame frame, T value, ApplyType type = ApplyType.Edit);
}

public class CommandManager
{
+   public static DateTime LastExecutedTime { get; }
}

+public sealed class GradientProperty : PropertyElement<GradientPropertyMetadata>
+{
+    public GradientProperty(GradientPropertyMetadata metadata);

+    public ObservableCollection<GradientKeyPoint> KeyPoints { get; }

+    public IRecordCommand AddPoint(GradientKeyPoint keyPoint);
+    public IRecordCommand RemovePoint(GradientKeyPoint keyPoint);
+    public IRecordCommand UpdatePoint(int index, GradientKeyPoint keyPoint);
+}

+public readonly struct GradientKeyPoint
+{
+    public GradientKeyPoint(Color color, float position);

+    public Color Color { get; }
+    public float Position { get; }

+    public static GradientKeyPoint Parse(string s);
+    public static bool TryParse(string? s, out GradientKeyPoint result);
+}

public interface IApplication
{
+   public ObservableCollection<BasePluginMenu> DisplayedMenus { get; }
}

-public interface ICustomMenu

-public sealed class CustomMenu

+public enum MenuLocation
+{
+    Default,
+    Left,
+    Right,
+    Bottom,
+}

+public abstract class BaseMenu<T> : EditingObject
+{
+    public static readonly EditingProperty<object?> MainWindowProperty;

+    public event EventHandler<T>? Clicked;

+    public object? MainWindow { get; set; }

+    public void Execute(T arg);
+    protected virtual void OnExecute(T arg);
+}

+public abstract class BasePluginMenu : BaseMenu<object?>
+{
+    public static readonly EditingProperty<MenuLocation> MenuLocationProperty;

+    public MenuLocation MenuLocation { get; set; }

+    public void Execute();
+    protected abstract void OnExecute();
+    protected sealed override void OnExecute(object? arg);
+}

+public class FileMenu : BaseMenu<string>
+{
+    public static readonly EditingProperty<IReadOnlyList<string>> SupportedExtensionsProperty;

+    public IReadOnlyList<string> SupportedExtensions { get; set; }

+    public bool IsMatch(string file);
+}

public class PluginBuilder
{
-   public PluginBuilder SetCustomMenu(string header, IEnumerable<ICustomMenu> menus);
+   public PluginBuilder PluginMenu(string header, params BasePluginMenu[] menus);
+   public PluginBuilder FileMenu(FileMenu menus);
+   public PluginBuilder Language(SupportedLanguage supportedLanguage);
}

BEditor.Drawing
public class Image
{
+   public static void PixelOperate<TOperation>(this Image<BGRA32> image, DrawingContext context, params object[] args);
+   [Obsolete]
    public static void PixelOperate<TOperation, TArg>(this Image<BGRA32> image, DrawingContext context, TArg arg);
+   [Obsolete]
    public static void PixelOperate<TOperation, TArg1, TArg2>(this Image<BGRA32> image, DrawingContext context, TArg1 arg1, TArg2 arg2);
+   [Obsolete]
    public static void PixelOperate<TOperation, TArg1, TArg2, TArg3>(this Image<BGRA32> image, DrawingContext context, TArg1 arg1, TArg2 arg2, TArg3 arg3);
+   [Obsolete]
    public static void PixelOperate<TOperation, TArg1, TArg2, TArg3, TArg4>(this Image<BGRA32> image, DrawingContext context, TArg1 arg1, TArg2 arg2, TArg3 arg3, TArg4 arg4);
+   public static Image<BGRA32> Arc(int width, int height, float startAngle, float sweepAngle, bool useCenter, Brush brush);
-   public static Image<BGRA32> Text(string text, Font font, float size, Color color, HorizontalAlign hAlign, VerticalAlign vAlign, float linespace = 0);
-   public static Image<BGRA32> StrokeText(string text, Font font, float size, float strokewidth, Color color, HorizontalAlign hAlign, float linespace = 0);
}

+[Obsolete]
public interface IGpuPixelOperation<T>
{
    public string GetSource();
    public string GetKernel();
}

+[Obsolete]
public interface IGpuPixelOperation<T1, T2>
{
    public string GetSource();
    public string GetKernel();
}

+[Obsolete]
public interface IGpuPixelOperation<T1, T2, T3>
{
    public string GetSource();
    public string GetKernel();
}

+[Obsolete]
public interface IGpuPixelOperation<T1, T2, T3, T4>
{
    public string GetSource();
    public string GetKernel();
}

BEditor.Graphics
public struct Transform
{
-   public Vector3 Coordinate { get; set; }
-   public Vector3 Rotate { get; set; }
}

public class GraphicsContext
{
+   [Obsolete]
    public void DrawLine(Vector3 start, Vector3 end, float width, Transform transform, Color color);
+   [Obsolete]
    public void DrawLine(Line line);
}

BEditor.Media
public interface IRegisteredDecoding
{
+   [Obsolete]
    public IEnumerable<string> SupportExtensions();
+   public IEnumerable<string> GetSupportedVideoExt();
+   public IEnumerable<string> GetSupportedAudioExt();
}

public interface IAudioStream
{
-   public Sound<StereoPCMFloat> GetFrame(TimeSpan time, TimeSpan duration);
}

public sealed class VideoStreamInfo
{
-   public VideoStreamInfo(string codecName, MediaType type, TimeSpan duration, Size framesize, int framenum, int framerate);

-   public int FrameRate { get; }
}

public sealed class Sound<T>
{
-   public Sound(int rate, TimeSpan duration);

-   public Sound<T> Slice(TimeSpan start);
-   public Sound<T> Slice(TimeSpan start, TimeSpan length);
}

BEditor.Primitive
public static class PrimitiveTypes
{
-   public static readonly Type Video;
-   public static readonly Type Audio;
-   public static readonly Type Image;
-   public static readonly Type Text;
-   public static readonly Type Shape;
-   public static readonly Type Polygon;
-   public static readonly Type RoundRect;
-   public static readonly Type Camera;
-   public static readonly Type GL3DObject;
-   public static readonly Type Scene;
-   public static readonly Type Framebuffer;
}

BEditor.Settings
+public sealed class SupportedLanguage : EditingObject, IEquatable<SupportedLanguage?>
+{
+    public static readonly EditingProperty<CultureInfo> CultureProperty;
+    public static readonly EditingProperty<string> DisplayNameProperty;
+    public static readonly EditingProperty<string> BaseNameProperty;
+    public static readonly EditingProperty<Assembly> AssemblyProperty;

+    public SupportedLanguage(CultureInfo culture, string displayName, string baseName, Assembly assembly)

+    public static bool operator ==(SupportedLanguage? left, SupportedLanguage? right)
+    public static bool operator !=(SupportedLanguage? left, SupportedLanguage? right)

+    public static SupportedLanguage Default { get; }
+    public CultureInfo Culture { get; }
+    public string DisplayName { get; }
+    public string BaseName { get; }
+    public Assembly Assembly { get; }

+    public overide bool Equals(object? obj);
+    public bool Equals(SupportedLanguage? other);
+    public override int GetHashCode();
+}

public sealed class Settings
{
-   public static readonly EditingProperty<string> LanguageProperty;
+   public static readonly EditingProperty<ObservableCollection<SupportedLanguage>> SupportedLanguagesProperty;
+   public static readonly EditingProperty<SupportedLanguage> LanguageProperty;

-   public string Language { get; set; }
+   public SupportedLanguageLanguage { get; set; }
+   public ObservableCollection<SupportedLanguage> SupportedLanguages { get; set; }
}
```