name: Daily build
on:
  schedule:
    - cron: 0 0 * * * # Every day at midnight (UTC)
jobs:
  daily-build:
    runs-on: windows-latest
    strategy:
      matrix:
        rid: [linux-x64, win-x64, osx-x64]
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x

    - name: Restore dependencies
      run: dotnet restore BEditorNext.sln

    - name: Build
      run: dotnet publish ./src/BEditorNext/BEditorNext.csproj -c Release -r ${{matrix.rid}} --no-self-contained

    - name: Save
      uses: actions/upload-artifact@v2
      with:
        name: beditor_${{matrix.rid}}
        path: ./src/BEditorNext/bin/Release/net6.0/${{matrix.rid}}/publish
