#include "BeutlAVFTypes.h"

// Placeholder translation unit so SwiftPM recognizes this as a buildable C target.
// All shared types live in the header; no runtime code is needed here.
const int _beutl_avf_types_marker = 1;
