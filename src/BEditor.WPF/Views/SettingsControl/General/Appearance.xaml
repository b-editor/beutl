<UserControl
    x:Class="BEditor.Views.SettingsControl.General.Appearance"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:convert="clr-namespace:BEditor.ViewModels.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="clr-namespace:BEditor;assembly=BEditor.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:resources="clr-namespace:BEditor.Properties"
    xmlns:vm="clr-namespace:BEditor.ViewModels.SettingsControl.General"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:AppearanceViewModel />
    </UserControl.DataContext>
    <VirtualizingStackPanel>


        <md:ColorZone Content="{x:Static resources:Strings.General}" Style="{StaticResource SettingsColorZone}" />

        <VirtualizingStackPanel Height="45" Orientation="Horizontal">
            <CheckBox
                Command="{Binding UseDarkModeClick, Mode=OneTime}"
                IsChecked="{Binding UseDarkMode, Source={x:Static data:Settings.Default}, Mode=TwoWay}"
                Style="{StaticResource SettingsCheckBox}" />
            <TextBlock Style="{StaticResource SettingsCheckBoxContent}" Text="{x:Static resources:Strings.UseDarkMode}" />
        </VirtualizingStackPanel>

        <VirtualizingStackPanel Height="45" Orientation="Horizontal">
            <CheckBox IsChecked="{Binding ShowStartWindow, Source={x:Static data:Settings.Default}, Mode=TwoWay}" Style="{StaticResource SettingsCheckBox}" />
            <TextBlock Style="{StaticResource SettingsCheckBoxContent}" Text="{x:Static resources:Strings.ShowStartWindow}" />
        </VirtualizingStackPanel>


        <md:ColorZone Content="{x:Static resources:Strings.Timeline}" Style="{StaticResource SettingsColorZone}" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <VirtualizingStackPanel>
                <Label Content="{x:Static resources:Strings.ClipHeight}" Style="{StaticResource SettingsHeader}" />
                <Label Content="{x:Static resources:Strings.WidthOf1Frame}" Style="{StaticResource SettingsHeader}" />
                <Label Content="{x:Static resources:Strings.Language}" Style="{StaticResource SettingsHeader}" />
            </VirtualizingStackPanel>
            <VirtualizingStackPanel Grid.Column="1">
                <TextBox Style="{StaticResource SettingsTextBox}" Text="{Binding ClipHeight, Source={x:Static data:Settings.Default}, Mode=TwoWay}" />
                <TextBox Style="{StaticResource SettingsTextBox}" Text="{Binding WidthOf1Frame, Source={x:Static data:Settings.Default}, Mode=TwoWay}" />
                <ComboBox
                    ItemsSource="{Binding Langs, Mode=OneTime}"
                    SelectedItem="{Binding Language, Source={x:Static data:Settings.Default}, Mode=TwoWay}"
                    Style="{StaticResource SettingsComboBox}" />
            </VirtualizingStackPanel>
        </Grid>
    </VirtualizingStackPanel>
</UserControl>
