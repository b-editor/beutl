<UserControl x:Class="BEditor.Views.SettingsControl.License"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BEditor.Views.SettingsControl"
             xmlns:vm="clr-namespace:BEditor.ViewModels.SettingsControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:LicenseViewModel/>
    </UserControl.DataContext>
    <ScrollViewer>
        <ItemsControl ItemsSource="{Binding Licenses, Mode=OneTime}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Margin="8" FontSize="24" Text="{Binding Library, Mode=OneTime}" FontWeight="Bold"/>
                        <TextBlock Margin="8" FontSize="14" Text="{Binding Text, Mode=OneTime}" TextWrapping="WrapWithOverflow"/>

                        <Border HorizontalAlignment="Stretch" Background="{DynamicResource MaterialDesignBody}" Height="1" Margin="8"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
