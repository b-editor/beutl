<UserControl x:Class="BEditor.Views.StartWindowControl.Learn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BEditor.Views.StartWindowControl"
             xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:BEditor.ViewModels.StartWindowControl"
             xmlns:convert="clr-namespace:BEditor.ViewModels.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:LearnViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="{x:Type dragablz:TabablzControl}" BasedOn="{StaticResource MaterialDesignTabablzControlStyle}">
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="dragablz:DragablzItem" BasedOn="{StaticResource MaterialDesignDragableTabItemStyle}">
                        <Setter Property="Height" Value="30" />
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ItemsControl Visibility="{Binding IsLoaded.Value, Converter={StaticResource BooleanToVisibilityConverter}}" ItemsSource="{Binding Items, Mode=OneTime}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <RadioButton
                        Click="RadioButton_Checked"
                        GroupName="TabButton"
                        Style="{StaticResource MaterialDesignTabRadioButton}"
                        Margin="4"
                        IsChecked="{Binding IsSelected.Value, Mode=TwoWay}"
                        Content="{Binding Header, Mode=OneTime}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Horizontal" Margin="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <ScrollViewer Visibility="{Binding IsLoaded.Value, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.Row="1">
            <ItemsControl ItemsSource="{Binding SelectedItem.Value.Pages, Mode=OneWay}" PreviewMouseWheel="ItemsControl_PreviewMouseWheel">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Foreground="{DynamicResource MaterialDesignBody}">
                            <Expander.Header>
                                <TextBlock Text="{Binding Header, Mode=OneTime}"/>
                            </Expander.Header>
                            <FlowDocumentScrollViewer Document="{Binding Document, Mode=OneTime}"/>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Visibility="{Binding IsNotLoaded.Value, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
