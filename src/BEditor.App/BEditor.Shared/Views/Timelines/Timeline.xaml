<UserControl
    x:Class="BEditor.Shared.Views.Timelines.Timeline"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BEditor.Shared.Views.Timelines"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="1000">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ScrollViewer x:Name="ScrollLine" Grid.Row="0" Grid.Column="1" HorizontalScrollBarVisibility="Visible" Grid.RowSpan="2">
            <Grid Height="Auto" HorizontalAlignment="Left" x:Name="timelinegrid" Width="1920">

                <StackPanel
                    x:Name="Layer"
                    HorizontalAlignment="Left">
                    <Grid x:Name="scale" HorizontalAlignment="Left" Height="32" />
                </StackPanel>
                <Border Background="Red" Width="1.25" HorizontalAlignment="Left"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
